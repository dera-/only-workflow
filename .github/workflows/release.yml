name: Publish And Release

on:
  workflow_dispatch:
    inputs:
      git_repository:
        description: Git repository
        required: true
      target_branch:
        description: Target branch
        required: true
        default: master
      target_tag:
        description: Target tag
        required: true
        default: latest
      additional_target_tag:
        description: npm の publish 時に target_tag に加えて付与される tag である。利用例：akashic-engine v3 系のライブラリを publish する時に latest タグとは別に for_ae3x タグにも残したい場合。
        required: false

env:
  NODE_VERSION: 12

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this repository
        uses: actions/checkout@v2
        with:
          path: workflows
      - name: Checkout target repository
        uses: actions/checkout@v2
        with:
          repository: dera-/${{ github.event.inputs.git_repository }}
          ref: ${{ github.event.inputs.target_branch }}
          path: target
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: ${{ env.NODE_VERSION }}
      - run: npm install
        working-directory: workflows
      - name: Prepare publish
        run: |
          npm install
          npm test
        working-directory: target
      - name: Get npm token
        id: npm_token
        uses: ./workflows/getNpmToken
        with:
          repository_path: ${{ github.workspace }}/target
      - name: Publish
        uses: JS-DevTools/npm-publish@v1
        with:
          token: ${{ steps.npm_token.outputs.token }}
          package: ./target/package.json
          tag: ${{ github.event.inputs.target_tag }}
          access: public
      - name: Read package.json
        id: package_json
        run: echo "::set-output name=content::$(cat ./package.json | tr -d '\n')"
        working-directory: target
      - name: Add dist tag
        if: ${{ github.event.inputs.additional_target_tag }}
        run: npm dist-tag add ${{ fromJson(steps.package_json.outputs.content).name }}@${{ fromJson(steps.package_json.outputs.content).version }} ${{ github.event.inputs.additional_target_tag }}
        env:
          INPUT_TOKEN: ${{ steps.npm_token.outputs.token }}
      - name: Generate release note
        id: release_note
        uses: ./workflows/generateReleaseNote
        with:
          repository_path: ${{ github.workspace }}/target
      - name: Create release note
        uses: actions/create-release@master
        env:
          # 他リポジトリに対して操作を行う場合secret.GITHUB_TOKENとは別にgithubトークンを利用する必要があるためsecret.PERSONAL_ACCESS_TOKENを用意
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        with:
          tag_name: ${{ steps.release_note.outputs.version }}
          release_name: Release ${{ steps.release_note.outputs.version }}
          body: ${{ steps.release_note.outputs.body }}
          draft: false
          prerelease: false
          commitish: ${{ github.event.inputs.target_branch }}
          owner: dera-
          repo: ${{ github.event.inputs.git_repository }}
